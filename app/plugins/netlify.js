export const commands = ['netlify']

export default async function({selected, query}) {
  debugger
  // convert DOM to a stringified version
  // post to netlify
  // show indeterminate upload UI (nice to have)
  // success: open new tab to new netlify page
}
